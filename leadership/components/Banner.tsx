export default class Event {
    title: string;
    message: string;

    constructor(title, message) {
        this.title = title;
        this.message = message;
    }

    displayBanner() {
        // Code for function goes here
    }
}